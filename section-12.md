# Databases Services you should know

## AWS DocumentDB

- Document is the noSQL db base on mongodb
- its scale the workload millions of requests/sec
- storeage automatically grow

## AWS ElasticCache

- its ame like RDS from aws,
- you can create elasticache for high performace of the db
- usecase, users session can be stored in cache db
- usecase, there could be static value which needs to call again and again we can store to cache

## Vally

- support vector db
- it can scale

## Memdb

- its also vector db support
- it can have two index : flat and HSD which uses in opensearch

## AWS Neptune

- its highly useful for graph db
- its can be use for relationshipt database


## AWS Neptune with GenAi

- this can be have vector database, if there is graph database that's can be use for vectordb

