# Managing Model with Sagemake AI

## Sagemaker to handle the entire machine learning workflow

- you can train the model base on data
- evalue the performance of the model after training
- all things base on containers so it will be on ECRs
- you can also deploy blue green deployment of model so some users can be new deployment some are in old models
- you can train and tune model in sagemaker AI and de3ploy to custom models
- each model can get their own scaling policy
- also there is cross region inference
- we can also get smaller model from bigger model

## SageMaker Model and Monitor

- you can monitor the output of the sagemake model and alert it
- clerify also help us to understand if the resutl are bais

## Sagemaker model registry

- you can store the model catalog
- you can also get approval before deployment
- this can be in CICD
- this can be use for share the models

## SageMaker ML Lineage Tracking

- create and store workflows
- store model
- Trail component (training)
- trail (mode)
- things can be connected
- 